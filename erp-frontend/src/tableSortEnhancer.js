const normalize=text=>String(text||'').replace(/\s+/g,' ').trim();const valueOf=text=>{const t=normalize(text),date=t.match(/^(\d{1,2})[./-](\d{1,2})[./-](\d{4})$/);if(date)return{type:'number',value:new Date(`${date[3]}-${date[2]}-${date[1]}`).getTime()};const cleaned=t.replace(/[^\d,.-]/g,'').replace(/\.(?=\d{3}(?:\D|$))/g,'').replace(',','.');if(cleaned&&Number.isFinite(Number(cleaned)))return{type:'number',value:Number(cleaned)};return{type:'text',value:t.toLocaleLowerCase('tr-TR')}};const compare=(a,b)=>{const x=valueOf(a),y=valueOf(b);if(x.type==='number'&&y.type==='number')return x.value-y.value;return String(x.value).localeCompare(String(y.value),'tr',{numeric:true,sensitivity:'base'})};const sortTable=th=>{if(th.dataset.noSort==='true'||th.querySelector('button,input,select'))return;const table=th.closest('table'),headRow=th.parentElement,tbody=table?.tBodies?.[0];if(!table||!tbody||!headRow)return;const headers=[...headRow.cells],index=headers.indexOf(th);if(index<0)return;const direction=th.dataset.sort==='asc'?'desc':'asc';headers.forEach(h=>{delete h.dataset.sort;h.removeAttribute('aria-sort')});th.dataset.sort=direction;th.setAttribute('aria-sort',direction==='asc'?'ascending':'descending');[...tbody.rows].sort((a,b)=>{const result=compare(a.cells[index]?.innerText,b.cells[index]?.innerText);return direction==='asc'?result:-result}).forEach(row=>tbody.appendChild(row))};const onClick=e=>{const th=e.target.closest('th');if(th&&th.closest('table'))sortTable(th)};const mark=()=>document.querySelectorAll('table thead th').forEach(th=>{if(th.dataset.noSort!=='true'&&!th.querySelector('button,input,select')){th.classList.add('alya-sortable');th.tabIndex=0;th.title='Sıralamak için tıklayın'}});if(typeof document!=='undefined'){document.addEventListener('click',onClick);document.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&e.target.matches('th.alya-sortable')){e.preventDefault();sortTable(e.target)}});new MutationObserver(mark).observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mark);else mark()}
