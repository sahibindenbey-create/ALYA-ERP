const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');test('030 tam veri aynası şirket izolasyonlu ve idempotent tablolar kurar',()=>{const s=read('sql/030_KOLAYBI_FULL_MIRROR.sql');for(const x of['KolaybiFullSyncRuns','KolaybiRawMirror','ContentHash','UQ_KolaybiRawMirror','operation=1','operation=2','fn_CompanyIsolationPredicate'])assert.match(s,new RegExp(x));assert.doesNotMatch(s,/ApiKey|AccessToken/)});test('Tam ayna yalnız Yamankaya ve açık yönetici onayıyla çalışır',()=>{const s=read('core/kolaybiFullMirrorRoutes.js');for(const x of["cid!==2","YAMANKAYA","YAMANKAYA_FULL_MIRROR","requirePermission('core.admin')","KOLAYBI_FULL_MIRROR","sale_return_invoice","purchase_return_invoice","associate_transactions","vault_transactions"])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(s,/console\.log/)});test('Tam aktarım ekranı plan ve açık onay sunar',()=>{const s=read('../erp-frontend/src/components/KolaybiFullSyncPanel.js');assert.match(s,/Tam aktarım planı/);assert.match(s,/Tüm ham veriyi aktar/);assert.match(s,/YAMANKAYA_FULL_MIRROR/);assert.match(read('core/costingFlowRoutes.js'),/kolaybiFullMirrorRoutes/)});
