const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');test('Faz 15 migration ATP ve stok politikasını güvenli kurar',()=>{const s=read('sql/026_AVAILABLE_STOCK_ATP_CHAIN.sql');for(const x of['StokPolitikalariV2','AtpKontrolleriV2','vw_SatilabilirStokV2','AcikSatisMiktari','AcikSatinAlmaMiktari','AcikUretimMiktari','ProjeksiyonStok'])assert.match(s,new RegExp(x));assert.doesNotMatch(s,/\bDROP\s+TABLE\b|\bTRUNCATE\b/i)});test('Faz 15 servisleri şirket yetkisi, politika ve ATP kontrolü içerir',()=>{const s=read('core/availabilityFlowRoutes.js');for(const x of['createAuthMiddleware','requirePermission','writeAudit','/overview','/policies/:productId','/atp-check','STOCK_POLICY_UPSERT'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(read('core/costingFlowRoutes.js'),/availabilityFlowRoutes/)});test('Faz 15 satılabilir stok arayüzü menüye bağlıdır',()=>{const panel=read('../erp-frontend/src/pages/AvailabilityControlPanel.js'),routes=read('../erp-frontend/src/pages/ModulePlaceholder.js');assert.match(panel,/Satılabilir Stok Merkezi/);assert.match(panel,/\/availability/);assert.match(routes,/availabilityPaths/);assert.match(routes,/AvailabilityControlPanel/)});
